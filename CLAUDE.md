# Reading Log

読書記録アプリ。GitHub Pagesでホスティング。

## 技術スタック

- HTML (index.html) + CSS (style.css) + JS (app.js)
- Vanilla JavaScript / 外部依存なし

## ローカルストレージ

**ユーザーデータを失わないこと**

- `STORAGE_KEY` (`'readingLogV4'`) を変更しない
- 既存プロパティの削除・リネーム・型変更をしない
- 新プロパティ追加時はデフォルト値を設定
